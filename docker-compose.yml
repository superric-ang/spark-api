version: '3.8'
services:
  api:
    build: ./apps/api
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      - NODE_ENV=development

  matching:
    build: ./services/matching
    ports:
      - "8001:8001"
    env_file: .env
    environment:
      - PYTHONPATH=/app